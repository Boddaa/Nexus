<UserControl x:Class="Nexus.Desktop.Views.BoardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="📋 Study &amp; Kanban Boards" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="Manage learning pipelines, topic masteries, and task items." FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <!-- Board Columns Grid -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Column 1: TO LEARN -->
            <Border Grid.Column="0" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="14" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="TO LEARN" FontWeight="Bold" Foreground="{StaticResource AccentRoseBrush}" Margin="0,0,0,12" />
                    <Border Background="{StaticResource BgHoverBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <TextBlock Text="• Relationships &amp; Foreign Keys" FontSize="13" />
                    </Border>
                    <Border Background="{StaticResource BgHoverBrush}" CornerRadius="6" Padding="10">
                        <TextBlock Text="• EF Migrations &amp; CLI" FontSize="13" />
                    </Border>
                </StackPanel>
            </Border>

            <!-- Column 2: LEARNING -->
            <Border Grid.Column="1" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="14" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="LEARNING" FontWeight="Bold" Foreground="{StaticResource AccentAmberBrush}" Margin="0,0,0,12" />
                    <Border Background="{StaticResource BgHoverBrush}" CornerRadius="6" Padding="10">
                        <TextBlock Text="• DbContext Lifetime &amp; DI" FontSize="13" />
                    </Border>
                </StackPanel>
            </Border>

            <!-- Column 3: REVIEW -->
            <Border Grid.Column="2" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="14" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="REVIEW" FontWeight="Bold" Foreground="{StaticResource AccentCyanBrush}" Margin="0,0,0,12" />
                    <Border Background="{StaticResource BgHoverBrush}" CornerRadius="6" Padding="10">
                        <TextBlock Text="• IQueryable vs IEnumerable" FontSize="13" />
                    </Border>
                </StackPanel>
            </Border>

            <!-- Column 4: MASTERED -->
            <Border Grid.Column="3" Background="{StaticResource BgCardBrush}" CornerRadius="8" Padding="14" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="MASTERED" FontWeight="Bold" Foreground="{StaticResource AccentEmeraldBrush}" Margin="0,0,0,12" />
                    <Border Background="{StaticResource BgHoverBrush}" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <TextBlock Text="• SQL Joins &amp; Projections" FontSize="13" />
                    </Border>
                    <Border Background="{StaticResource BgHoverBrush}" CornerRadius="6" Padding="10">
                        <TextBlock Text="• Entity Framework Setup" FontSize="13" />
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
