<UserControl x:Class="Nexus.Desktop.Views.AiAssistantView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="350">
    <Border Background="{StaticResource BgSidebarBrush}"
            BorderBrush="{StaticResource BorderSubtleBrush}"
            BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource BgDarkestBrush}" Padding="16,14" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🤖" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0" />
                    <TextBlock Text="NEXUS AI Assistant" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Messages Stream -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="14">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" Padding="12,10" Margin="0,0,0,12"
                                    Background="{StaticResource BgCardBrush}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Role}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentPrimaryBrush}" Margin="0,0,0,4" />
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Input Bar -->
            <Border Grid.Row="2" Background="{StaticResource BgDarkestBrush}" Padding="14" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding InputMessage, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,8,0" />

                    <Button Grid.Column="1"
                            Content="➤"
                            Command="{Binding SendMessageCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Width="40" Height="40"
                            Padding="0" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
