<UserControl x:Class="Nexus.Desktop.Views.StudyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🎓 AI Study Engine &amp; Tutor" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Text="AI-generated quizzes, spaced repetition flashcards, and weak-topic analysis." FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Quizzes Panel -->
            <Border Grid.Column="0" Style="{StaticResource ModernCard}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="📝 Practice Quizzes" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource AccentAmberBrush}" Margin="0,0,0,12" />
                    <TextBlock Text="Generate multi-choice and short answer tests from your documents." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16" />
                    <Button Content="Generate New Quiz" Style="{StaticResource PrimaryButton}" Height="38" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <!-- Flashcards Panel -->
            <Border Grid.Column="1" Style="{StaticResource ModernCard}" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="🗂️ Spaced Repetition Flashcards" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource AccentCyanBrush}" Margin="0,0,0,12" />
                    <TextBlock Text="Review concepts using the SM-2 adaptive algorithm." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16" />
                    <Button Content="Start Flashcard Review" Style="{StaticResource SecondaryButton}" Height="38" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
